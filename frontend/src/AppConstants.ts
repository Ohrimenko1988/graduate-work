export const AppConstants = {
    BASE_REQUEST_URL: process.env.BASE_REQUEST_URL || "http://localhost:8080",
    HOT_TOURS_URI: process.env.HOT_TOURS_URI || "/hot-tours",
    SEARCH_TOURS_URI: process.env.SEARCH_TOURS_URI || "/search",

    RESULT_PAGE_PATH: process.env.RESULT_PAGE_PATH || "/results"
}
